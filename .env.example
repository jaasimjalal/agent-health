# Server Configuration
PORT=3000
NODE_ENV=development
LOG_LEVEL=info
SERVICE_VERSION=1.0.0

# Dependency Checks (set to true to enable actual checks)
DB_CHECK=false
API_CHECK=false
CACHE_CHECK=false

# External Service URLs (for actual checks)
DATABASE_URL=postgresql://localhost:5432/health
EXTERNAL_API_URL=https://api.example.com/health
